server:
  port: '9001'


  spring:
    application:
      name:

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/expenses
    password: mysql
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'

